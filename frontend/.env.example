# Optional. Override backend origin for SSE and other direct browser → API calls.
# If unset during `next dev`, SSE uses http://localhost:8000 (bypasses Next proxy buffering).
# For `next start` / production, set to your public API URL when frontend and API differ.
# NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
